// Add these to your existing server.js file\n\n// Add session middleware (add this after express setup)\nconst session = require('express-session');\nconst MongoStore = require('connect-mongo');\n\n// Session configuration\napp.use(session({\n  secret: process.env.SESSION_SECRET || 'your-secret-key',\n  resave: false,\n  saveUninitialized: false,\n  store: MongoStore.create({\n    mongoUrl: process.env.MONGODB_URI || 'mongodb://localhost:27017/zyncjobs'\n  }),\n  cookie: {\n    secure: false, // Set to true in production with HTTPS\n    httpOnly: true,\n    maxAge: 24 * 60 * 60 * 1000 // 24 hours\n  }\n}));\n\n// Add meeting routes\nconst meetingRoutes = require('./routes/meetings');\napp.use('/api/meetings', meetingRoutes);\n\n// Install required packages:\n// npm install express-session connect-mongo googleapis axios\n\n// Package.json dependencies to add:\n/*\n\"express-session\": \"^1.17.3\",\n\"connect-mongo\": \"^5.0.0\",\n\"googleapis\": \"^126.0.1\",\n\"axios\": \"^1.6.0\"\n*/